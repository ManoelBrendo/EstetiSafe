{
    "name": "lappui-backend",
    "version": "1.0.0",
    "main": "server.js",
    "engines": {
        "node": ">=18"
    },
    "scripts": {
        "dev": "nodemon server.js",
        "start": "node server.js",
        "migrate": "prisma migrate dev",
        "deploy": "prisma migrate deploy",
        "studio": "prisma studio",
        "seed:demo": "node scripts/seed-demo.js",
        "migrate:anamnesis-legacy": "node scripts/migrate-legacy-anamnesis.js",
        "lint": "eslint server.js lib tests scripts --ext js",
        "test": "node --test tests/*.test.js",
        "test:integration": "node --test tests/integration/*.test.js",
        "backfill:clinic-aggregates": "node scripts/backfill-clinic-aggregates.js"
    },
    "dependencies": {
        "@prisma/client": "^5.0.0",
        "bcryptjs": "^2.4.3",
        "cors": "^2.8.5",
        "dotenv": "^16.4.5",
        "express": "^4.18.2",
        "jsonwebtoken": "^9.0.2",
        "pdfkit": "^0.18.0",
        "prisma": "^5.0.0",
        "zod": "^3.22.4"
    },
    "devDependencies": {
        "eslint": "^8.57.0",
        "nodemon": "^3.0.1"
    }
}
